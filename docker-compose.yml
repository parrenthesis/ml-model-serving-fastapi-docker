services:
  api:
    build:
      context: .
    image: housing-api:latest
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=/app/model
      - DATA_DIR=/app/data
      - INPUT_EXTRA_POLICY=allow
    restart: unless-stopped
